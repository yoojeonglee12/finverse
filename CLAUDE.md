<!-- FINVERSE-DOC · AI 어시스트 작성 · 원본 소스(README·app/·tools/·worker/)와 구분되는 산출물 -->
> 🟦 이 파일은 Claude Code가 매 세션 읽는 프로젝트 가이드입니다. 상세는 `docs/`. · 최종 갱신 2026-08-07

# FINVERSE — 프로젝트 가이드

## 한 줄 정의
사용자가 던진 질문에 대해 지금 시장 상황을 온톨로지로 구조화하고 에이전트 시뮬레이션으로 **조건부 미래 시나리오**를 만들어, 그 앞에서 자기 판단을 시험하며 인지 편향을 발견하게 하는 서비스. (2026 금융 AI Challenge 출품)

**파는 것은 예측값이 아니라 조건이다.** "KOSPI가 7,500 간다"가 아니라 "외국인 수급이 돌아오고 실적이 컨센서스를 웃돌 때**에만**" 그 경로가 열린다고 말한다.

## 핵심 흐름
```
사용자 질문 → 검색 범위 결정 → 4영역 정보 수집 → 현재 시장 상황 온톨로지 구성
           → MiroFish 시뮬레이션 → 미래 시나리오
```

## 확정된 구조
- **4개 정보 영역** — 시장(무엇이 움직였나) / 경제(어떤 환경인가) / 외부 사건(원인은 무엇인가) / **사람들의 심리**(어떻게 반응하나). 심리는 시장의 결과이자 원인 — 유일한 양방향.
- **6단계 파이프라인** — ①Ontology Generation ②GraphRAG Build ③Generate Agent Profiles ④Generate Config ⑤Initial Activation ⑥Scenario Simulation. UI는 구현됨, **백엔드 전부 미구현**.
- **그래프 스키마** — 엔터티 7종(지수·종목·섹터·이벤트·환율·수급·금리) / 관계 6종(`DRIVES` `IMPACTS` `TRACKS` `CORRELATES_WITH` `CONSTRAINS` `TRIGGERS`). 규모 42노드·68엣지. **관계 6종의 의미는 아직 미정의.**
- **에이전트** — 엔터티에서 파생, 24명. 결과는 3인 해석(뉴스 수집가·애널리스트·퀀트)으로 노출되고 시나리오 성격별 가중치가 다름.
- **두 탭** — 시장 인사이트(정보제공) / 마이 금융 트윈(상호작용). **시나리오가 두 탭을 잇는 유일한 객체.**
- **히스토리컬** — 최소 1년 일별 OHLCV, 큰 움직임의 원인 분석, 과거 유사 사례 대조. 단 **과거 결과를 미래의 정답으로 쓰지 않고 현재 조건과의 차이를 함께 표시.**

## 문서 지도
- **PRD (현행 기준점)**: `docs/PRD.md` — **v0.6** (온톨로지 기준 전면 재작성)
- **온톨로지 원본**: `docs/ontology/finverse-ontology.md` + SVG 2종 — 4개 영역 정의 · 권장 수집원 · 검색 원칙
- **화면 목업**: `demo/finverse-mockup.html` — S0 빌더 · S1 빌드 6단계 · V1·V2·V3 + 데이터 바인딩/API 주석
- MiroFish 코드해부: `docs/MiroFish-코드해부-리포트.html` (OASIS + Zep Cloud, AGPL-3.0, 비재현·토큰폭식)
- 기획서(원본): `README.md`
- ⚠️ **v0.5 산출물**(섹터 9개 · 관계DB 73행 · 2-hop 계산 · 회의록 #1~#21)은 fork의 `planning-docs` 브랜치에 보존. 이 브랜치에는 없음.

## 지금 상태
- **화면 5개 중 4개가 이미 구현됨** (`app/page.tsx`). 없는 것은 `Scenario`를 실제로 만드는 백엔드와 **트윈 입력 화면**.
- **병목은 프론트가 아니라 백엔드다.** 시나리오 3건은 하드코딩, 트윈은 목데이터.
- ⚠️ **최대 리스크 — 캐시가 성립하지 않는다.** 질문마다 온톨로지를 새로 만들면 캐시 키가 없고, 에이전트 24 × 라운드 24 = **질문 1건당 수백 회 LLM 호출**이 유저 수에 선형 비례한다. 완화안은 PRD §9.1.

## 오픈 이슈 (PRD §11)
1. 관계 타입 6종의 **의미 정의** — 부호·강도를 어디에 담나 (그래프 빌드 차단)
2. 24 에이전트 → 3인 해석 **집계 규칙** · 가중치 결정 주체
3. **트윈 입력 화면 부재** — 온보딩 + 무보유 유저 템플릿
4. **캐시 전략**
5. 대가 로스터 변경 — **"트럼프식 시장 관점"** 실존 정치인 실명, 면책 수위
6. AGPL 의무 범위 · 7. MiroFish **재현성**(시드 고정) · 8. 히스토리컬 유사 사례 매칭 규칙

## 기술 스택 / 실행
- Next.js 16 + React 19 + Vite(vinext) + Tailwind 4, Cloudflare Worker 배포.
- 실행: `uv run python tools/site.py dev` → `http://localhost:3000` (Node 22.13+).

## 작업 컨벤션
- **생성하는 기획·설계 문서는 상단에 `<!-- FINVERSE-DOC ... -->` 배너**를 달고 `docs/`·`demo/` 아래에 둔다 (원본 git 파일과 구분).
- **차트 색: 빨강 = 상승 / 파랑 = 하락** (한국 시장 관례).
- **용어**: 비유형 조어 금지 → 도메인 용어 사용: **온톨로지 · 그래프/GraphRAG · 환경 시드 · 에이전트 프로필 · 시나리오 · 반증 신호 · 기여도**.
- **안전 원칙**: 가격·수익·시장방향 **단정 금지** → 조건부 밴드. 모든 시나리오에 **반증 신호 필수**. 실계좌 연동 없음(가상 프로필). 상품 추천 금지. 대가 페르소나 면책 라벨 상시. **커뮤니티 데이터는 사실이 아니라 심리 관측값.**
