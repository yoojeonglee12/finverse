<!-- FINVERSE-DOC · AI 어시스트로 작성한 기획·설계 문서 (원본 소스코드와 구분) -->
> 🟦 **FINVERSE 기획·설계 문서** — AI 어시스트 작성. 원본 소스(README · app/ · tools/ · worker/)와 **구분되는 산출물**입니다.
> 이 파일은 Claude Code가 매 세션 읽는 프로젝트 가이드입니다. 상세는 `docs/`를 참고하세요. · 최종 갱신 2026-07-31

# FINVERSE — 프로젝트 가이드

## 한 줄 정의
**매일 갱신되는 시장 세계관(Base Model)** 위에서 실제 뉴스 사건을 시뮬레이션해 보여주고, 사용자가 그 속에서 자기 판단을 시험하며 인지 편향을 발견하는 AI 금융 판단 훈련 서비스. (2026 금융 AI Challenge 출품)

## 현재 상태 (2026-08-01 회의 이후)
- **3트랙 병렬 착수 (#20)** — ① F/E 랜딩(뉴스 수집 + 시뮬 연결, 담당 미확정) · ② 데이터 수집 백엔드(신민수) · ③ 가상 트윈 화면 데모 + 프론트 연결(이유정). **앱 서버는 아직 미할당.**
- **시뮬레이션 방식 미합의 (#21)** — 각자 최소 버전을 만들어 본 뒤 합치기로. 통합 전제는 `EventSimulation` 출력 스키마(PRD §6.3) 공통 확정, 그중 **`sectorImpact`가 임계점**.
- ⚠️ **에이전트 시뮬은 저장소에 미구현.** 데모의 타임라인은 하드코딩, LLM SDK 의존성 없음, MiroFish는 코드 해부만 함. 결정 #18의 경량화 가정은 **미검증**.
- ⚠️ 8/1 준비 안건(브리핑 §7 A/B/C)은 **논의되지 않고 이월**됨. **A-3(이벤트 카드 선별 기준)·A-4(트윈 캐릭터 항목)가 각각 ①·③ 트랙 착수 직후 병목**.

## 확정된 제품 방향 (근거: `docs/회의록/` · 결정 #1~#21)
- **4층 구조 (#14)** — ⓪외부 데이터 연동(매일) → ①**Base Model**(읽기전용 세계관, 매일 재구성) → ②**이벤트 시뮬**(뉴스 카드 클릭 시 생성) → ③**나 트윈**(유저 상호작용).
- **A·B·C = 같은 Base Model을 좁혀 쓰는 3단계** — C 시장 인사이트(세계 전체) → B 사회실험(사건 하나로 흔든 세계) → A 판단 훈련(내 것만 하이라이트). **①·② = 정보제공 / ③ = 상호작용.** 흐름 **C→B→A**, 비중 **A > C > B**.
- **3계층 온톨로지 (#15)** — 매크로(지표·기관/정부·경제이벤트) / 섹터 9개 MECE / 종목 KOSPI 100. **그래프 엣지는 매크로↔섹터 사이에만**, 종목은 섹터 멤버십만. 유저 자산 영향은 `매크로 → 섹터 → 종목 비중` 2-hop 계산.
- **관계DB 분리 (#16)** — 관계는 그래프에 인라인하지 않고 별도 DB에 **방향·부호·강도(3단계)·근거**로 저장, 그래프는 참조해 렌더링만. **근거 없는 정밀 수치(0.73 등) 금지.** 규모 73행.
- **갱신 주기 분리 (#17)** — state(지표 수치·이벤트)는 **매일**, 관계(인과 구조)는 **분기 또는 구조 변화 시**.
- **이벤트 카드 단위 온디맨드 생성 + 캐시 (#18)** — 카드 최초 클릭 시 1회 생성 후 캐시(키 = 카드 id + BaseModel 날짜). 경량 시뮬(매크로 agent 10~15 + 섹터 agent 9, 라운드 5~8). 유저 요청마다 재실행 금지는 캐시로 유지.
- **대가 4인 = ③ 나 트윈 전용 (#19)** — 워런 버핏·레이 달리오·캐시 우드·하워드 막스. **② 이벤트 시뮬에는 참여하지 않음.** "대표 투자자라면 어떻게 판단했을까"를 시뮬해 보는 참고·엔터 요소. 실명 + 강한 면책 라벨 상시.
- **무결성 (#8)** — Base Model·시뮬 캐시는 읽기전용(L1), 유저 기록은 트윈(L2)에만. 유저가 쓸 수 있는 공유 그래프를 만들지 않음.

## 문서 지도
- **PRD (현행 기준점)**: `docs/PRD.md` — **v0.5**(4층 아키텍처 · 섹터 계층 · 관계DB · 온디맨드+캐시 · 모듈 4덩어리)
- **8/1 회의 브리핑 (개발팀 설명용)**: `docs/2026-08-01-회의-브리핑.md` — §0 세 문장 정의 + 활용 기술·데이터 흐름·기능 모듈 · 서비스 시나리오 3분 · 4층 설계 · 데이터 계약 4개 · 분담 · **안건 A/B/C 그룹 + 각 권장안**
  - 보기용 사본: `docs/2026-08-01-회의-브리핑.html` (`.md`를 렌더링한 것 — **내용은 `.md`에서 고치고 재생성**, 방법은 HTML 상단 주석 참조)
- **구조 재정의 회의록**: `docs/회의록/2026-07-31-new-구조-재정의.md` — 결정 #14~#19 + 기존 #3·#9·#11·#12 개정
- **섹터·관계DB 초안**: `docs/섹터-종목매핑-관계DB-초안.md` — **섹터 9개** · KOSPI 100 매핑 · **관계DB 73행**(8/1 안건 1·2 검토용)
- **작업 분담 회의록 (최신)**: `docs/회의록/2026-08-01-작업-분담.md` — 결정 **#20~#21** · 3트랙 분담 · 시뮬 분산 프로토타입 · **이월된 안건 표** · 통합 리스크와 완화책
- **회의록**: `docs/회의록/` — 0725 킥오프 · 07-29(#1~#9) · 07-31 시나리오 정렬(#10~#13) · 07-31-new 구조 재정의(#14~#19) · 08-01 작업 분담(#20~#21)
- 기획서(원본): `README.md` — 결정 #1~#13 기준 (⚠️ #14~#19 미반영, 개정 대기)
- 시나리오 문서: `docs/시나리오-반도체-랠리-변곡점.md` — ⚠️ v0.4 구조 기반, 상당 부분 대체됨(문서 상단 배너 참조)
- MiroFish 코드해부 리포트: `docs/MiroFish-코드해부-리포트.html` (엔진 실체: OASIS + Zep Cloud, AGPL-3.0, 단일프로세스·비재현·토큰폭식)
- 데모(프로토타입): **v0.5 구조로 재작성 완료**
  - `demo/finverse-world.html` — Base Model 세계관 그래프. 이벤트7·기관7·지표10·섹터9·종목100, **관계DB 73행을 엣지로 렌더**(부호=색/강도=굵기), 엣지 hover 시 근거 표시, 지표 클릭 시 2-hop 충격 전파 계산
  - `demo/finverse-simulation.html` — **C→B→A 흐름**. V1 이벤트 카드(캐시/온디맨드 생성 UX) → V2 매크로·섹터 에이전트 타임라인 + 섹터 충격 팩터 + 3경로 **범위** → V3 대가 4인 참고 + 내 선택 + 편향 판정
  - `demo/finverse-mockup.html` — **구현용 화면 목업 + 데이터 바인딩 명세**. S0 트윈 설정 · V1 · V2 · V3 와이어프레임 각 블록에 소비 계약(`BaseModel`/`RelationDB`/`EventSimulation`/`UserTwin`)·API·P0/P1 주석. API 8개 + 화면×계약 매트릭스 + 공통 컴포넌트 규칙

## 다음 결정 (2026-08-01 회의 안건 · **상세·권장안: `docs/2026-08-01-회의-브리핑.md` §7**)
**A그룹 — 개발 착수를 막고 있는 것 (오늘 결정)**
1. 섹터 **9개 안 승인** + 결정 #15의 "7~8개"를 9개로 조정한 사실 기록 · S-Oil 유가 상충은 `factorOverride`로 처리 권장
2. 관계DB 73행 검토 + **강도(strength) 실증 계획** 승인 · `regulates` 어휘 추가 여부(추가 권장)
3. 이벤트 카드 선별 기준 (권장: 하루 3~5개 · 관계DB 엔티티 언급 뉴스만 · MVP 수동 선별)
4. 나 트윈 캐릭터 설정 항목 + **무보유 유저 템플릿 포트폴리오 3종**

**B그룹 — 방향만**
5. 대가 시뮬의 엔터 요소 수위 + 실명 사용 권리 조항 확인
6. 편향 판정에 "왜 그렇게 판단했나" 한 줄 입력 포함 여부 (권장: 포함)

**C그룹 — 담당·기한만**
7. 경량 agent 시뮬 생성 시간·토큰 실측 (결정 #18 타당성 검증) — **가장 중요한 숙제**
8. AGPL 의무 범위 법률 확인 (§13 네트워크 조항)
9. **【신규】 간접 경로(2-hop) 전파 규칙** — `WTI → CPI → 소비재`처럼 직접 엣지가 없는 경로를 섹터 충격에 반영할지. 권장: MVP는 직접 엣지만, 간접은 근거 문장으로만 노출

※ 구 안건이던 **outcome 저장 방식·전망 표기·progressive disclosure**는 구조 변경으로 해소됨(PRD §12).

## 기술 스택 / 실행
- Next.js 16 + React 19 + Vite(vinext) + Tailwind 4, Cloudflare Worker 배포.
- 실행: `uv run python tools/site.py dev` → `http://localhost:3000` (Node 22.13+).

## 작업 컨벤션
- **생성하는 기획·설계 문서는 상단에 `<!-- FINVERSE-DOC ... -->` 배너**를 달고 `docs/`·`demo/` 아래에 둔다 (원본 git 파일과 구분).
- **차트 색: 빨강 = 상승 / 파랑 = 하락** (한국 시장 관례).
- **용어**: 비유형 조어 금지(척추·얼린·공장·촬영↔상영 등) → 도메인 용어 사용: **Base Model · 관계DB · 이벤트 카드 · 섹터 충격 팩터 · 트레이스 · 리플레이 · 캐시 · 파이프라인 · 무결성(L1/L2)**. 회의록의 구 용어는 소급 수정하지 않음.
- **안전 원칙**: 가격·수익·시장방향 단정 금지 → 조건부 범위·신뢰구간. 실계좌 연동 없음(가상 프로필). 상품 추천 금지. 페르소나 면책 라벨 상시.
