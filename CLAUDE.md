<!-- FINVERSE-DOC · AI 어시스트로 작성한 기획·설계 문서 (원본 소스코드와 구분) -->
> 🟦 **FINVERSE 기획·설계 문서** — AI 어시스트 작성. 원본 소스(README · app/ · tools/ · worker/)와 **구분되는 산출물**입니다.
> 이 파일은 Claude Code가 매 세션 읽는 프로젝트 가이드입니다. 상세는 `docs/`를 참고하세요. · 최종 갱신 2026-07-31

# FINVERSE — 프로젝트 가이드

## 한 줄 정의
시장 사건을 대가 4인과 군중이 반응하는 가상세계로 **미리 시뮬레이션해 두고**, 사용자가 그 속에서 자기 판단을 시험하며 인지 편향을 발견·교정하는 AI 금융 판단 훈련 서비스. (2026 금융 AI Challenge 출품)

## 확정된 제품 방향 (근거: `docs/회의록/`)
- **핵심 기능 = 에이전트 사회 시뮬레이션**(A 판단훈련 + B 사회실험). 가격 예측(C)은 연결된 진입 모듈. 비중 **A > C > B**.
- **base = 3계층 온톨로지** — Force(매크로 지표) / Persona(대가·군중) / Instrument(종목).
- **대가 4인 실명** — 워런 버핏·레이 달리오·캐시 우드·하워드 막스 + 강한 면책(실존 발언 아님, 공개 원칙 기반 교육용). 종토방 군중은 배경 OASIS 군중.
- **운영 = MiroFish는 오프라인 시나리오 생성 파이프라인.** 시뮬레이션은 팀이 오프라인에서 1회 실행·기록하고 **시나리오 스냅샷**(요약 + 그래프 스냅샷 + 상호작용 트레이스 · 읽기전용)으로 저장 → 서비스는 리플레이·서빙 + 경량 개인화만. 유저 요청마다 라이브 시뮬 금지.
- **나 트윈 = 출력 뷰**(유저 계층 L2, 지속·누적). 무결성 = 공유 그래프(L1)는 읽기전용, 유저 기록은 트윈(L2)에만 — 유저가 쓸 수 있는 공유 그래프를 안 만듦.
- **월드 그래프 = A의 근거(프로버넌스) 레이어 · B의 관찰 뷰** — 전면에 내세우는 주 기능 아님. Progressive disclosure: 기본은 내 보유 종목 관련 노드만, [전체 세계 펼치기]로 KOSPI 100종목.

## 문서 지도
- 기획서(원본): `README.md` — 결정 #1~#13 반영 개정 완료(2026-07-31): 멀티에이전트 시뮬·스냅샷·분기 선택형 UX·면책 정식 반영
- **PRD**: `docs/PRD.md` (v0.4 — 스냅샷 계약·기능 모듈·mermaid 아키텍처)
- **메인 시나리오**: `docs/시나리오-반도체-랠리-변곡점.md` — MVP 시나리오 전체(타임라인 T1~T8 · 분기 · 피드백 설계, 미확정은 【제안】 표시)
- **회의록**: `docs/회의록/YYYY-MM-DD-*.md`
- MiroFish 코드해부 리포트: `docs/MiroFish-코드해부-리포트.html` (엔진 실체: OASIS + Zep Cloud, AGPL-3.0, 단일프로세스·비재현·토큰폭식)
- 데모(프로토타입): `demo/finverse-simulation.html`(S1→S2→S3 흐름), `demo/finverse-world.html`(가상세계 그래프)

## 다음 결정 (2026-08-01 회의 안건 · 상세: PRD §12, 시나리오 문서 §10)
1. 변곡점 수(1개 vs 2~3개) · 선택지 수(개정 README는 5개 유지·"지켜본다" 포함, PRD 부록 A는 4개 — 정합화 필요)
2. 편향 판정 입력에 "왜 그렇게 판단했나" 한 줄 포함 여부 (M6)
3. 스냅샷 outcome 재설계 — 유저용 고정 숫자 대신 시장 팩터 + 트윈 계산
4. 무보유 유저 온보딩(템플릿 포트폴리오) · MVP 클레임 수위("교정"→"발견")
5. 실명 페르소나 — 공모전 권리 조항 확인 + 아키타입 폴백 스위치

※ README 개정(구 안건 4)은 2026-07-31 완료 — 조건 조정 UX를 분기 선택형으로 통일.

## 기술 스택 / 실행
- Next.js 16 + React 19 + Vite(vinext) + Tailwind 4, Cloudflare Worker 배포.
- 실행: `uv run python tools/site.py dev` → `http://localhost:3000` (Node 22.13+).

## 작업 컨벤션
- **생성하는 기획·설계 문서는 상단에 `<!-- FINVERSE-DOC ... -->` 배너**를 달고 `docs/`·`demo/` 아래에 둔다 (원본 git 파일과 구분).
- **차트 색: 빨강 = 상승 / 파랑 = 하락** (한국 시장 관례).
- **용어**: 비유형 조어 금지(척추·얼린·공장·촬영↔상영 등) → 도메인 용어 사용: **시나리오 스냅샷 · 트레이스 · 리플레이 · 배치 생성 · 파이프라인 · 무결성(L1/L2)**. 회의록의 구 용어는 소급 수정하지 않음.
- **안전 원칙**: 가격·수익·시장방향 단정 금지 → 조건부 범위·신뢰구간. 실계좌 연동 없음(가상 프로필). 상품 추천 금지. 페르소나 면책 라벨 상시.
